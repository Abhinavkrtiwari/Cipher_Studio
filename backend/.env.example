# Backend environment variables (example)
# Replace <username>, <password>, and <cluster-url> with your MongoDB Atlas details
MONGODB_URI=mongodb+srv://<username>:<password>@<cluster-url>/cipherschoolassignment?retryWrites=true&w=majority

# Port your server listens on
PORT=5000

# JWT secret for auth (generate a long random string for production)
JWT_SECRET=your_jwt_secret_here

# Optional: set NODE_ENV=production in production environments
NODE_ENV=development
